package com.adept.ims.authorization.database.dao.impl;

import java.util.List;

import java.util.Map;
import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import com.adept.ims.authorization.database.dao.RoleServiceDAO;
import com.adept.ims.authorization.database.pojo.RoleService;

/**
 * This class interacts with Spring and Hibernate to save and
 * retrieve Trust objects.
 */
public class RoleServiceDAOImpl extends AuthorizationDAOImpl implements RoleServiceDAO {

    private static Log log = LogFactory.getLog(RoleServiceDAOImpl.class);

    /**public RoleServiceDAOImpl() {
        className = RoleService.class.getName();
    }**/

    public RoleService get(long roleServiceId) {
        return (RoleService) super.get(roleServiceId);
    }

    public List<RoleService> getAll() {
        return super.getAll();
    }


    public void save(RoleService roleService) {
        super.save(roleService);
    }

    public void update(RoleService roleService) {
        super.update(roleService);
    }
 public List<RoleService> getByFilter(String filter, String value) {
        return super.getByFilter(filter, value);
    }

    public List<RoleService> getByOperator(String operator, Map<String, String> queryMap) {
        return super.getByOperator(operator, queryMap);
    }
    
}
